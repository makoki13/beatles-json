import 'package:flutter/material.dart';

class CancionesBuscarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buscar Canciones'),
      ),
      body: Center(
        child: Text('Página de búsqueda de canciones'),
      ),
    );
  }
}