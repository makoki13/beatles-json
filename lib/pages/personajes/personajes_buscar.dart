import 'package:flutter/material.dart';

class PersonajesBuscarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buscar Personajes'),
      ),
      body: Center(
        child: Text('Página de búsqueda de personajes'),
      ),
    );
  }
}