import 'package:flutter/material.dart';

class PersonajesAddPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agregar Personaje'),
      ),
      body: Center(
        child: Text('Página para agregar un nuevo personaje'),
      ),
    );
  }
}